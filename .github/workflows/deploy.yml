name: Deploy

on:
  push:
    branches: [main]

# Prevent concurrent deploys — if a new push lands while deploying, cancel the
# in-progress run and start fresh.
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  ECR_BACKEND_REPO: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/stocktracker-backend

jobs:
  # ── Build & push Docker images to ECR ───────────────────────────────────────
  docker-push:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    outputs:
      image-tag: ${{ steps.meta.outputs.tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set image tag
        id: meta
        run: echo "tag=${{ github.sha }}" >> "$GITHUB_OUTPUT"

      - name: Build and push backend image
        run: |
          docker build \
            -f apps/backend/Dockerfile \
            -t $ECR_BACKEND_REPO:${{ steps.meta.outputs.tag }} \
            -t $ECR_BACKEND_REPO:latest \
            .
          docker push $ECR_BACKEND_REPO:${{ steps.meta.outputs.tag }}
          docker push $ECR_BACKEND_REPO:latest

  # ── Deploy frontend to S3 + invalidate CloudFront (staging) ─────────────────
  frontend-staging:
    name: Deploy Frontend (Staging)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Install dependencies
        run: npm ci

      - name: Build frontend (staging)
        run: npm run build -w apps/frontend
        env:
          VITE_API_URL: ${{ secrets.STAGING_API_URL }}
          VITE_WS_URL: ${{ secrets.STAGING_WS_URL }}

      - name: Sync to S3
        run: |
          aws s3 sync apps/frontend/dist/ s3://${{ secrets.STAGING_FRONTEND_BUCKET }} \
            --delete \
            --cache-control "public,max-age=31536000,immutable" \
            --exclude "index.html"
          # index.html must not be cached so clients always get the latest shell
          aws s3 cp apps/frontend/dist/index.html s3://${{ secrets.STAGING_FRONTEND_BUCKET }}/index.html \
            --cache-control "no-cache,no-store,must-revalidate"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.STAGING_CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"

  # ── OpenTofu plan + apply (staging) ─────────────────────────────────────────
  deploy-staging:
    name: Deploy Staging Infrastructure & ECS
    runs-on: ubuntu-latest
    needs: [docker-push, frontend-staging]
    environment: staging
    outputs:
      cluster-name: ${{ steps.tofu-outputs.outputs.cluster_name }}
      service-name: ${{ steps.tofu-outputs.outputs.service_name }}
      migrate-task-arn: ${{ steps.tofu-outputs.outputs.migrate_task_arn }}
      private-subnets: ${{ steps.tofu-outputs.outputs.private_subnets }}
      ecs-sg-id: ${{ steps.tofu-outputs.outputs.ecs_sg_id }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: '1.9.0'

      - name: OpenTofu Init
        working-directory: infra
        run: |
          tofu init \
            -backend-config="bucket=${{ secrets.TF_BACKEND_BUCKET }}" \
            -backend-config="key=stocktracker/staging/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_BACKEND_LOCK_TABLE }}"

      - name: OpenTofu Apply (staging)
        working-directory: infra
        run: tofu apply -auto-approve -var-file=envs/staging.tfvars
        env:
          TF_VAR_backend_image_tag: ${{ needs.docker-push.outputs.image-tag }}
          TF_VAR_acm_certificate_arn: ${{ secrets.STAGING_ACM_CERT_ARN }}
          TF_VAR_cloudfront_certificate_arn: ${{ secrets.STAGING_CF_CERT_ARN }}

      - name: Read OpenTofu outputs
        id: tofu-outputs
        working-directory: infra
        run: |
          echo "cluster_name=$(tofu output -raw ecs_cluster_name)" >> "$GITHUB_OUTPUT"
          echo "service_name=$(tofu output -raw ecs_service_name)" >> "$GITHUB_OUTPUT"
          echo "migrate_task_arn=$(tofu output -raw migrate_task_definition_arn)" >> "$GITHUB_OUTPUT"
          echo "private_subnets=$(tofu output -raw private_subnet_ids)" >> "$GITHUB_OUTPUT"
          echo "ecs_sg_id=$(tofu output -raw ecs_security_group_id)" >> "$GITHUB_OUTPUT"

      - name: Run DB migrations (ECS one-off task)
        run: |
          TASK_ARN=$(aws ecs run-task \
            --cluster ${{ steps.tofu-outputs.outputs.cluster_name }} \
            --task-definition ${{ steps.tofu-outputs.outputs.migrate_task_arn }} \
            --launch-type FARGATE \
            --network-configuration "awsvpcConfiguration={subnets=[${{ steps.tofu-outputs.outputs.private_subnets }}],securityGroups=[${{ steps.tofu-outputs.outputs.ecs_sg_id }}],assignPublicIp=DISABLED}" \
            --query 'tasks[0].taskArn' \
            --output text)
          echo "Waiting for migration task $TASK_ARN..."
          aws ecs wait tasks-stopped \
            --cluster ${{ steps.tofu-outputs.outputs.cluster_name }} \
            --tasks "$TASK_ARN"
          EXIT_CODE=$(aws ecs describe-tasks \
            --cluster ${{ steps.tofu-outputs.outputs.cluster_name }} \
            --tasks "$TASK_ARN" \
            --query 'tasks[0].containers[0].exitCode' \
            --output text)
          echo "Migration exit code: $EXIT_CODE"
          test "$EXIT_CODE" = "0"

      - name: Force ECS service redeploy
        run: |
          aws ecs update-service \
            --cluster ${{ steps.tofu-outputs.outputs.cluster_name }} \
            --service ${{ steps.tofu-outputs.outputs.service_name }} \
            --task-definition $ECR_BACKEND_REPO:${{ needs.docker-push.outputs.image-tag }} \
            --force-new-deployment

      - name: Wait for ECS service stability
        run: |
          aws ecs wait services-stable \
            --cluster ${{ steps.tofu-outputs.outputs.cluster_name }} \
            --services ${{ steps.tofu-outputs.outputs.service_name }}

      - name: Smoke test staging
        run: |
          curl --fail --retry 5 --retry-delay 10 \
            ${{ secrets.STAGING_API_URL }}/health

  # ── Playwright E2E against staging ──────────────────────────────────────────
  e2e-staging:
    name: E2E Tests (Staging)
    runs-on: ubuntu-latest
    needs: deploy-staging
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
        working-directory: apps/e2e

      - name: Run E2E tests
        run: npx playwright test --project=chromium
        working-directory: apps/e2e
        env:
          PLAYWRIGHT_BASE_URL: ${{ secrets.STAGING_FRONTEND_URL }}
          API_URL: ${{ secrets.STAGING_API_URL }}
          E2E_USER_EMAIL: ${{ secrets.E2E_USER_EMAIL }}
          E2E_USER_PASSWORD: ${{ secrets.E2E_USER_PASSWORD }}
          E2E_PREMIUM_EMAIL: ${{ secrets.E2E_PREMIUM_EMAIL }}
          E2E_PREMIUM_PASSWORD: ${{ secrets.E2E_PREMIUM_PASSWORD }}
          E2E_ADMIN_EMAIL: ${{ secrets.E2E_ADMIN_EMAIL }}
          E2E_ADMIN_PASSWORD: ${{ secrets.E2E_ADMIN_PASSWORD }}

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: apps/e2e/playwright-report/
          retention-days: 7

  # ── Deploy to production (manual approval gate) ──────────────────────────────
  deploy-production:
    name: Deploy Production
    runs-on: ubuntu-latest
    needs: e2e-staging
    environment: production    # GitHub requires manual approval for this environment
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy frontend (production)
        run: |
          npm ci
          npm run build -w apps/frontend
          aws s3 sync apps/frontend/dist/ s3://${{ secrets.PROD_FRONTEND_BUCKET }} \
            --delete \
            --cache-control "public,max-age=31536000,immutable" \
            --exclude "index.html"
          aws s3 cp apps/frontend/dist/index.html s3://${{ secrets.PROD_FRONTEND_BUCKET }}/index.html \
            --cache-control "no-cache,no-store,must-revalidate"
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.PROD_CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"
        env:
          VITE_API_URL: ${{ secrets.PROD_API_URL }}
          VITE_WS_URL: ${{ secrets.PROD_WS_URL }}

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: '1.9.0'

      - name: OpenTofu Init
        working-directory: infra
        run: |
          tofu init \
            -backend-config="bucket=${{ secrets.TF_BACKEND_BUCKET }}" \
            -backend-config="key=stocktracker/prod/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_BACKEND_LOCK_TABLE }}"

      - name: OpenTofu Apply (production)
        working-directory: infra
        run: tofu apply -auto-approve -var-file=envs/prod.tfvars
        env:
          TF_VAR_backend_image_tag: ${{ needs.docker-push.outputs.image-tag }}
          TF_VAR_acm_certificate_arn: ${{ secrets.PROD_ACM_CERT_ARN }}
          TF_VAR_cloudfront_certificate_arn: ${{ secrets.PROD_CF_CERT_ARN }}

      - name: Read OpenTofu outputs
        id: tofu-outputs
        working-directory: infra
        run: |
          echo "cluster_name=$(tofu output -raw ecs_cluster_name)" >> "$GITHUB_OUTPUT"
          echo "service_name=$(tofu output -raw ecs_service_name)" >> "$GITHUB_OUTPUT"
          echo "migrate_task_arn=$(tofu output -raw migrate_task_definition_arn)" >> "$GITHUB_OUTPUT"
          echo "private_subnets=$(tofu output -raw private_subnet_ids)" >> "$GITHUB_OUTPUT"
          echo "ecs_sg_id=$(tofu output -raw ecs_security_group_id)" >> "$GITHUB_OUTPUT"

      - name: Run DB migrations (production)
        run: |
          TASK_ARN=$(aws ecs run-task \
            --cluster ${{ steps.tofu-outputs.outputs.cluster_name }} \
            --task-definition ${{ steps.tofu-outputs.outputs.migrate_task_arn }} \
            --launch-type FARGATE \
            --network-configuration "awsvpcConfiguration={subnets=[${{ steps.tofu-outputs.outputs.private_subnets }}],securityGroups=[${{ steps.tofu-outputs.outputs.ecs_sg_id }}],assignPublicIp=DISABLED}" \
            --query 'tasks[0].taskArn' \
            --output text)
          aws ecs wait tasks-stopped \
            --cluster ${{ steps.tofu-outputs.outputs.cluster_name }} \
            --tasks "$TASK_ARN"
          EXIT_CODE=$(aws ecs describe-tasks \
            --cluster ${{ steps.tofu-outputs.outputs.cluster_name }} \
            --tasks "$TASK_ARN" \
            --query 'tasks[0].containers[0].exitCode' \
            --output text)
          test "$EXIT_CODE" = "0"

      - name: Force ECS service redeploy
        run: |
          aws ecs update-service \
            --cluster ${{ steps.tofu-outputs.outputs.cluster_name }} \
            --service ${{ steps.tofu-outputs.outputs.service_name }} \
            --force-new-deployment

      - name: Wait for ECS service stability
        run: |
          aws ecs wait services-stable \
            --cluster ${{ steps.tofu-outputs.outputs.cluster_name }} \
            --services ${{ steps.tofu-outputs.outputs.service_name }}

      - name: Smoke test production
        run: |
          curl --fail --retry 5 --retry-delay 10 \
            ${{ secrets.PROD_API_URL }}/health
